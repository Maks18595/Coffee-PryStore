@{
    var currentLanguage = ViewData["CurrentLanguage"]?.ToString();
}

<html lang="@currentLanguage">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Raleway', sans-serif;
            background-color: #f8f3e9;
            color: #5a4635;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 100vh;
            padding: 0;
        }

      

        .content {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px 15px; /* Відступи для контенту */
        }

        .order-success-container {
            background: #ffffff;
            border: 1px solid #e0dcd2;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 30px;
            text-align: center;
            max-width: 500px;
            width: 100%;
        }

        h2 {
            font-size: 24px;
            color: #3e2f23;
            margin-bottom: 15px;
        }

        p {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .button {
            background-color: #8b5e3c;
            color: #fff;
            padding: 10px 25px;
            text-decoration: none;
            font-size: 14px;
            font-weight: bold;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            display: inline-block;
        }

            .button:hover {
                background-color: #6d4a2f;
            }

        .coffee-icon {
            font-size: 40px;
            margin-bottom: 15px;
        }

            .coffee-icon::before {
                content: "☕";
            }

       
    </style>
</head>
<body>

 

    <div class="content">
        <div class="order-success-container">
            <div class="coffee-icon"></div>
            <h2>Ваше замовлення успішно створене!</h2>
            <p>Дякуємо за покупку! Незабаром ми зв'яжемося з вами для підтвердження деталей.</p>
            <a href="/" class="button">Повернутись на головну</a>
        </div>
    </div>

</body>
</html>
